#include <stdio.h>

// Function declaration
float square(float num);

int main() {
    float number = 5.0;
    float result = square(number);
    printf("Square of %.2f is %.2f\n", number, result);
    return 0;
}

// Function definition
float square(float num) {
    return num * num;
}
